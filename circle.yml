dependencies:
   pre:
     - pip install tox

test:
   override:
     - tox -- --junitxml=$CIRCLE_TEST_REPORTS/junit/junit.xml --x-display=$DISPLAY -v -m "'not graphical'"

machine:
   python:
      version: 3.6.0
